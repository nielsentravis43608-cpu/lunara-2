"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ENERGY_META, ELEMENT_META, combination, EnergyKey } from "../../../lib/lunara-energy";
import { crystals } from "../../../lib/lunara";

export default function ResultPage(){
 const [reading,setReading]=useState<any>(null); const [email,setEmail]=useState(""); const [whatsapp,setWhatsapp]=useState(""); const [saved,setSaved]=useState(false); const [error,setError]=useState("");
 useEffect(()=>{try{setReading(JSON.parse(sessionStorage.getItem("lunara-energy-reading")||"null"))}catch{}},[]);
 const data=reading; const primary=data?.dominant as EnergyKey|undefined; const secondary=data?.secondary as EnergyKey|undefined;
 const combo=useMemo(()=>primary&&secondary?combination(primary,secondary):null,[primary,secondary]);
 async function saveLead(){setError(""); if(!email.includes("@")&&!whatsapp.trim()){setError("Please enter an email or WhatsApp number.");return} try{const r=await fetch("/api/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,whatsapp,name:"",dominant:ENERGY_META[primary!].label,secondary:ENERGY_META[secondary!].label,readingTitle:combo?.title,recommendedCrystals:[ENERGY_META[primary!].crystal,ENERGY_META[secondary!].crystal],source:typeof document!=="undefined"?document.referrer:"direct",utmSource:new URLSearchParams(location.search).get("utm_source")||"",utmCampaign:new URLSearchParams(location.search).get("utm_campaign")||"",testCompletedAt:data.completedAt})}); if(!r.ok)throw new Error(); setSaved(true)}catch{setError("We couldn't save your details. Please try again.")}}
 if(!data||!primary||!secondary)return <main className="empty shell"><p className="eyebrow">YOUR READING</p><h1>Your ritual is waiting.</h1><Link className="button button-dark" href="/reading">Begin the reading →</Link></main>;
 const meta=ENERGY_META[primary]; const secondaryMeta=ENERGY_META[secondary];
 return <main className="result-page"><header className="nav shell"><Link className="brand" href="/">LUNARA</Link><nav><Link href="/shop">Shop</Link><Link className="nav-cta" href="/cart">Cart</Link></nav></header><section className="result-hero shell"><p className="eyebrow">YOUR PERSONAL LUNARA PROFILE</p><h1>{combo?.title}</h1><p className="result-sub">{combo?.text}</p><div className="element-meter"><div className="meter-orb"><span>{meta.label}</span><strong>✦</strong></div><div className="meter-copy"><small>YOUR DOMINANT THEME</small><b>{meta.label} + {secondaryMeta.label}</b><div className="meter"><span style={{width:"72%"}}/></div><p>Your strongest theme is {meta.description.toLowerCase()}</p><p className="element-match"><small>FIVE ELEMENT MATCH</small><b>{meta.element} · {ELEMENT_META[meta.element].theme}</b></p></div></div></section><section className="profile-shell shell"><p className="eyebrow">THE READING</p><h2>A reflective mirror for the season you're in.</h2><div className="result-grid"><article><span>01 · DOMINANT</span><h3>{meta.archetype}</h3><p>{meta.description}</p></article><article><span>02 · SECONDARY</span><h3>{secondaryMeta.archetype}</h3><p>{secondaryMeta.description}</p></article><article><span>03 · BLEND</span><h3>{combo?.title}</h3><p>{combo?.text}</p></article></div></section><section className="ritual-card shell"><div className="ritual-copy"><p className="eyebrow">YOUR CRYSTAL MATCH</p><h2>{meta.crystal}</h2><p className="result-element-label">{meta.element} · {ELEMENT_META[meta.element].theme}</p><p className="tagline">A symbolic companion for your {meta.label.toLowerCase()} energy.</p><p>{meta.description} Explore the LUNARA collection for a physical reminder of the intention you want to carry forward.</p><div className="ritual-benefits"><span>✦ {meta.label}</span><span>✦ {secondaryMeta.label}</span><span>✦ Personal ritual</span></div><Link className="button button-dark" href="/shop">Discover your crystal →</Link></div><div className="ritual-art photo-art"><div className="energy-art"><span>✦</span><small>{meta.label} · LUNARA</small></div></div></section><section className="email-capture shell"><div><p className="eyebrow">KEEP YOUR READING</p><h2>Let the insight stay with you.</h2><p>Save your personalized reading to your inbox or WhatsApp. We only use your details for the LUNARA experience and opted-in follow-up.</p></div>{saved?<div className="saved">✦ Your contact details are saved.</div>:<div className="email-form"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address (optional)"/><input type="tel" value={whatsapp} onChange={e=>setWhatsapp(e.target.value)} placeholder="WhatsApp number (optional)"/><button className="button button-dark" onClick={saveLead}>Save my reading →</button>{error&&<p className="form-error">{error}</p>}</div>}</section><section className="reflection shell"><p className="eyebrow">ONE LAST QUESTION</p><h2>What would change if you trusted the next small step?</h2><Link className="text-link" href="/shop">Continue to your ritual →</Link></section><footer className="footer shell compact"><p>Keep what resonates. Leave what does not.</p><Link href="/reading">Take another reading</Link></footer></main>;
}
